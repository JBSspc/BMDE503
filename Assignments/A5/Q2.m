% Q2 returns the voltage output from a difference amplifier with ideal gain 'k'.
%
% y = Q2(V1, V2) returns k*(V1 - V2) when the subtraction is performed by a
% difference amplifier with finite common-mode rejection. (I.e., the
% subtraction is imperfect and contains error.)